"""
Airlock — a local PII redaction gate for consumer complaint text.

The library half of this project is deliberately empty at M0. Everything so far
is measurement, and measurement lives in scripts/ where each step is one file you
can read top to bottom. Code moves in here when a second caller actually needs it,
not before — see principle V of the constitution.
"""

__version__ = "0.0.1"
